# AGENTS.md

This repository's agent instructions live in **[CLAUDE.md](CLAUDE.md)**, with durable
project context in `memory/` and workstream detail in `docs/`.

`AGENTS.md` is the vendor-neutral convention read by most coding agents. `CLAUDE.md` is
the canonical file here; this pointer exists so a non-Claude agent finds it too. Keep the
content in CLAUDE.md, not duplicated here, so the two cannot drift apart.