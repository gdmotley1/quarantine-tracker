# Handoff index

A handoff is the entry point for **one workstream in flight**. Read the one that matches
the task. Durable truth lives in `memory/`; history lives in `docs/project-log.md`.

| Workstream | File | State |
|---|---|---|
| _none yet_ | | |

Keep a handoff to what a fresh session needs to resume: the TL;DR, current state, where each
number comes from, the rebuild command, and an explicit "do NOT" list. Promote anything
permanent into `memory/decisions.md` and leave a pointer.